const account_id = 82339487  // we cannot change value of const
let name = "Burhan"
var last_name = "Sandhu"
Status = "Student"
let City; // this will return undefined because we didn't assign it value

// we have to use 'let' not 'var'

console.log(account_id);

console.table([account_id, name, last_name, Status, City]); // to print multiple values in a table format

